export interface INavChildrenRes {
    id: number;
    name: string;
    src: URL;
    hasChildren: boolean;
}

export interface INavChildrenReq {
    id: number;
    // brand: string;
    // lang: string;
}
